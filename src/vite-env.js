

/**
 * @type {boolean} Whether the app is running in development mode
 */
// import.meta.env.DEV

/**
 * @type {boolean} Whether the app is running in production mode
 */
// import.meta.env.PROD

/**
 * @type {string} The mode the app is running in
 */
// import.meta.env.MODE

/**
 * @type {string} The base URL the app is being served from
 */
// import.meta.env.BASE_URL

/**
 * @type {Record<string, string>} Custom environment variables starting with VITE_
 */
// import.meta.env.VITE_*



export {}; // This makes the file a module
